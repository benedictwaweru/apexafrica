{
  "name": "web-admin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "web/admin/src",
  "projectType": "application",
  "targets": {
    "build": {
      "outputs": ["{projectRoot}/{options.outDir}", "{projectRoot}/dist"],
      "defaultConfiguration": "development",
      "options": {
        "config": "./vite.config.ts",
        "outDir": "../../dist/web/admin"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "defaultConfiguration": "development",
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["web/admin/**/*.{ts,tsx}"]
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "options": {
        "configFile": "web/admin/vite.config.ts"
      }
    },
    "docker:build": {
      "executor": "@nx-tools/nx-container:build",
      "options": {
        "file": "web/admin/Dockerfile",
        "context": "{workspaceRoot}",
        "target": "dev",
        "tags": ["apexafrica/admin-webclient:latest"]
      },
      "configurations": {
        "production": {
          "target": "prod",
          "tags": ["apexafrica.azurecr.io/admin-webclient:latest"],
          "push": true
        }
      }
    }
  }
}
